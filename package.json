{
  "name": "smarthealth",
  "version": "1.0.0",
  "description": "SmartHealth - Facturation Médicale (Sujet 13 - TP Qualimétrie)",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "lint": "eslint src/ --ext .js",
    "test": "jest --coverage --coverageReporters=lcov --coverageReporters=text"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "jest": {
    "testEnvironment": "node",
    "collectCoverageFrom": [
      "src/**/*.js",
      "!src/**/*.dirty.js"
    ]
  }
}
